{
  "name": "empty-checker",
  "version": "1.0.1",
  "description": "Npm package to check the value is worth. if the value is:  null, undefined, 0, ''. then the return is true and vice versa",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node",
    "npm"
  ],
  "author": "haryfun",
  "license": "MIT"
}
